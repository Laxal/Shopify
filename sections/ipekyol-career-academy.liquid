<div class="grid-section">
  <div class="grid-section__left" id="landing_menu_parent">
  </div>
  <div class="grid-section__right">
    <div>
      {%liquid
        assign title = section.settings.title
        
        if shop.locale == 'ar'
          if section.settings.title_ar != blank
          assign title = section.settings.title_ar
          endif 
        endif
        %}
        <div class="career-academy__title">
          <h1>{{title}}</h1>
        </div>
        <div>
          {% for block in section.blocks %}
            {% if section.blocks.size >0 %}
              {% if block.type == 'image'%}
                  <div class="ipekyol-card__image">
                    {% render 'media',image:block.settings.image %}
                  </div>
                  {% elsif block.type == 'text' %}
                  {%liquid
                    assign section_title = block.settings.section_title
                    assign text_id = block.settings.text_id
                  
                    if shop.locale == 'ar'
                        if block.settings.section_title_ar != blank
                        assign section_title = block.settings.section_title_ar
                      endif
                        if block.settings.text_id_ar != blank
                      assign text_id = block.settings.text_id_ar
                      endif
                    endif
                  %}

                  <div class="ayaydin_miroglio__item__text">
                    {% if section_title != blank %}
                      <h2 class="ayaydin_miroglio__item__text__title">{{section_title}}</h2>
                    {% endif %}
                    <div class="ayaydin_miroglio__item__text__rich">{{text_id}}</div>
                  </div>
              {%endif%}
            {%endif%}  
          {%endfor%}
        </div>
    </div>
  </div>
</div>
{% schema %}
  {
    "name": "ipekyol-career-academy",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Title",
        "default": "Ipekyol Career Academy"
      },
      {
        "type": "text",
        "id": "title_ar",
        "label": "Title (AR)"
      }
    ],
    "blocks":[
      {
        "type": "image",
        "name": "Image",
        "settings": [
          {
            "type": "image_picker",
            "id": "image",
            "label": "Section image"
          }
        ]
      },
      {
        "type": "text",
        "name": "Text",
        "settings":[
          {
            "type": "text",
            "id": "section_title",
            "label": "Section title"
          },
          {
            "type": "text",
            "id": "section_title_ar",
            "label": "Section title (AR)"
          },
          {
            "type": "richtext",
            "id": "text_id",
            "label": "Section text",
            "default": "<p>Ipekyol Career Academy was established in 2013 to support the personal and professional development of our employees. It has internal and external trainers<br><br>In the year it was founded, the Academy cooperated with Boğaziçi University Lifelong Education Center and realized a project for employment.<br><br>The employment project, with two terms and over 200 hours each term, is planned to show that retailing is a profession and offers career opportunities.<br><br>Graduates started to work as Specialist Sales Consultants, then they were promoted as Assistant Store Manager and Store Manager.</p>"
          },
          {
            "type": "richtext",
            "id": "text_id_ar",
            "label": "Section text (AR)"
          }
        ]
      }
    ]
  }
{% endschema %}